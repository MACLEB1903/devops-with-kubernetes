apiVersion: v1
kind: Service
metadata:
  name: pingpong-api-svc
  namespace: exercises
spec:
  type: LoadBalancer
  selector:
    app: pingpong-api
  ports:
    - port: 80
      protocol: TCP
      targetPort: 3000

---
apiVersion: v1
kind: Service
metadata:
  name: pingpong-db-svc
  namespace: exercises
spec:
  selector:
    app: pingpong-db
  clusterIP: None
